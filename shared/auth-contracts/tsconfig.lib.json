// RUTA: shared/auth-contracts/tsconfig.lib.json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // --- [INICIO DE CORRECCIÓN DE DELEGACIÓN SOBERANA] ---
    // Se aplica la misma corrección. Nx gestionará la ruta
    // de salida ('dist/shared/auth-contracts').
    // ANTES: "outDir": "../../dist/out-tsc",
    // --- [FIN DE CORRECCIÓN DE DELEGACIÓN SOBERANA] ---
    "declaration": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts"],
  "exclude": ["jest.config.ts", "src/**/*.spec.ts", "src/**/*.test.ts"]
}
