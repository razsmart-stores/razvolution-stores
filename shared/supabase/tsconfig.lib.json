{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  "exclude": [
    "jest.config.ts",
    "src/**/*.spec.ts",
    "src/**/*.test.ts",
    "src/**/*.spec.tsx",
    "src/**/*.test.tsx"
  ],
  "references": [
    // --- [INICIO DE CORRECCIÓN SOBERANA] ---
    // Se añade la referencia explícita a la configuración de build de 'shared-logging'.
    // Esto resuelve el error TS2307.
    {
      "path": "../logging/tsconfig.lib.json"
    },
    // --- [FIN DE CORRECCIÓN SOBERANA] ---
    {
      "path": "../db-types/tsconfig.lib.json"
    }
  ]
}
