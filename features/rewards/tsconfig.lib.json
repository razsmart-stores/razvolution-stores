{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "types": ["node"],
    // --- [INICIO DE CORRECCIÓN SOBERANA] ---
    // Se añade la propiedad "jsx" para que TypeScript pueda compilar
    // correctamente los archivos .tsx, resolviendo el error TS7026.
    "jsx": "react-jsx"
    // --- [FIN DE CORRECCIÓN SOBERANA] ---
  },
  // Se asegura de que los archivos .tsx estén incluidos en la compilación.
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  "exclude": [
    "jest.config.ts",
    "src/**/*.spec.ts",
    "src/**/*.test.ts",
    "src/**/*.spec.tsx",
    "src/**/*.test.tsx"
  ]
}
